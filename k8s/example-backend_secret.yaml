# TEMPLATE ONLY. Do not commit real secret values to git.
# Replace the placeholders below before applying, or better, generate this
# file locally with `kubectl create secret generic ...` and never check it in.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: habit-tracker
type: Opaque
stringData:
  MONGO_URL: "mongodb://mongo-service:27017/habit_tracker"
  JWT_SECRET: "string from env file"
  INTERNAL_SECRET: "string from env file"
