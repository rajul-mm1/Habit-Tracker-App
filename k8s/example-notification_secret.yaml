# TEMPLATE ONLY. Replace with a real email/push provider API key before use,
# and never commit the real value to git.
apiVersion: v1
kind: Secret
metadata:
  name: notification-secret
  namespace: habit-tracker
type: Opaque
stringData:
  RESEND_API_KEY: "generate this string from resend"
