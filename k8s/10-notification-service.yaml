apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: habit-tracker
spec:
  selector:
    app: notification-service
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
